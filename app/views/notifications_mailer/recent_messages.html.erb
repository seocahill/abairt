<!DOCTYPE html>
<html>
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type">
  </head>
  <body>
    <h1>New Messages in Abairt!</h1>
    <p>Hello <%= @user.email %>,</p>
    <p>We wanted to let you know that you have some new messages on abairt.com</p>
    <p>
      <ul>
        <% @messages.each do |message| %>
          <% next unless message.speaker %>
          <li>From <%= message.speaker.name %> <%= link_to "read", rang_url(message.rangs.last) %></li>
        <% end %>
      </ul>
    </p>
    <p>Thank you!</p>
  </body>
</html>
